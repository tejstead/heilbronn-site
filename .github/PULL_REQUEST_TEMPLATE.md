<!-- For coordinate submissions: add data/sources/external/<variant>-nNN/
     with coordinates.txt + meta.json — see CONTRIBUTING.md. The exact
     verifier will comment on this PR automatically. -->

## What this adds

<!-- e.g. "square n=17, found by simulated annealing, beats the page value" -->

## Provenance

- **Found by / source:**
- **Method (one line):**
- **Are these your own coordinates?** yes / no (whose?)

## Checklist

- [ ] `coordinates.txt` uses plain decimal literals (no exponents)
- [ ] `meta.json` has `ref` (and ideally `credit` + `note`)
- [ ] `make check-submission DIRS=...` passes locally (optional — CI runs it too)
